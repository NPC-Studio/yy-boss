global.anchorConfig = {
	
	// By default, all text is drawn onto a surface which is cached,
	// allowing for the game to not have to call draw_text / draw_text_ext
	// regularly. In my experiance this has been much more performant, but
	// if you'd prefer raw text draws, you can disable it here.
	surfaceBaking: true
}
